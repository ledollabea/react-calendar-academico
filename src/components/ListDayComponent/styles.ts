import styled from "styled-components";

const Container = styled.div`
 width: 100;
`;
const DayContainer = styled.div``;
const EventContainer = styled.div``;

export {Container, DayContainer, EventContainer}